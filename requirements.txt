# Core
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
python-dotenv==1.0.1

# Export
python-docx==1.1.2
reportlab==4.2.0

# Brief intake / OCR (Wizard step 0). Text-layer extraction needs no system
# deps; scanned-image OCR additionally needs the Tesseract binary installed.
# The intake pipeline degrades gracefully if any of these are unavailable.
pdfminer.six==20240706
pillow>=10.0
pytesseract==0.3.13
pdf2image==1.17.0

# Real auth — TOTP 2FA + observability (Phase 3).
pyotp==2.9.0
prometheus-client==0.20.0

# Vector store (production path). Local dev falls back to a pure-Python
# cosine store so the app runs with SQLite and zero native deps.
# psycopg[binary]==3.1.19
# pgvector==0.2.5

# LLM provider — OpenAI. The gateway falls back to a deterministic offline
# responder if the SDK/key is missing, so this can stay optional for offline demos.
openai>=2.0,<3.0
